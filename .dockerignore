# The build context needs exactly two files: both repos are cloned inside the
# build stages, so everything else is excluded. This keeps context transfer
# near-zero and can never leak a local .env into the image.
*
!Dockerfile
!docker-entrypoint.sh
